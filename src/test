private runCollisionCheckAndPan() {
  if (this.isPanning) return;

  const mapDiv = this.map.getDiv() as HTMLElement;
  const mapRect = mapDiv.getBoundingClientRect();

  // Get the VISIBLE card, not just the anchor container
  const cardElement = this.container.firstElementChild as HTMLElement;
  if (!cardElement) return;
  const cardRect = cardElement.getBoundingClientRect();

  const padding = 12;
  const topInset = 120;

  const safeTop = mapRect.top + topInset + padding;
  const safeLeft = mapRect.left + padding;
  const safeRight = mapRect.right - padding;
  const safeBottom = mapRect.bottom - padding;

  const outsideTop = cardRect.top < safeTop;
  const outsideLeft = cardRect.left < safeLeft;
  const outsideRight = cardRect.right > safeRight;
  const outsideBottom = cardRect.bottom > safeBottom;

  const hasCollision =
    outsideTop || outsideLeft || outsideRight || outsideBottom;
  if (!hasCollision) return;

  this.didPanForKey = this.keyForPos(this.position);
  this.isPanning = true;

  // Step 1: Center the marker
  this.map.panTo(this.position);

  // Step 2: After centering, check if card top is still clipped
  google.maps.event.addListenerOnce(this.map, 'idle', () => {
    const updatedCardRect =
      (this.container.firstElementChild as HTMLElement)?.getBoundingClientRect();
    const updatedMapRect =
      this.map.getDiv().getBoundingClientRect();
    const updatedSafeTop =
      updatedMapRect.top + topInset + padding;

    if (updatedCardRect && updatedCardRect.top < updatedSafeTop) {
      const nudgeY = updatedCardRect.top - updatedSafeTop;
      this.map.panBy(0, nudgeY);

      google.maps.event.addListenerOnce(this.map, 'idle', () => {
        this.isPanning = false;
      });
    } else {
      this.isPanning = false;
    }
  });
}
```

The **only change**: everywhere we used `this.container.getBoundingClientRect()`, we now use `this.container.firstElementChild.getBoundingClientRect()`.
```
this.container          → the invisible anchor at the marker point
this.container.firstElementChild → the .overlayAnchor div (the visible card)
